version: '3'
services:
  aleph-scoring:
    build: .
    image: aleph-scoring:latest
    command: "measure-on-schedule --publish"
    environment:
      SENTRY_DSN: ""
    volumes:
      - ./exports:/exports
